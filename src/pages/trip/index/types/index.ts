export interface musicSummary {
  songId: number;
  title: string;
  artist: string[];
  ranking: number;
  imgUrl: string;
}

export interface movieSummary {
  movieId: number;
  title: string;
  ranking: number;
  img: string;
}
